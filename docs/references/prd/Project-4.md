## 🔹 프로젝트 개요

- **기본 프로젝트**: 자율 주제 클라우드 아키텍처 설계 및 프로비저닝
- **심화 프로젝트**: 자율 주제 클라우드 네이티브 플랫폼 고도화 및 최적화
- **서비스 예시**: 팀 선정 자유 도메인 인프라 (AI 서빙, 게임 서버, 핀테크 등)

## 🔹 요약 (빠르게 보기)

| 구분 | 영역 | 필수 과업 | 선택 과업 |
| --- | --- | --- | --- |
| **기본** | **기획/설계** | 서비스 특성에 맞는 아키텍처 다이어그램 설계 | 비용 최적화 예측 모델링, 리스크 분석 |
|  | **구축** | IaC 기반 Full-Stack 인프라 구축, 컨테이너 배포 | 서버리스(Lambda/Functions) 부분 도입 |
|  | **검증** | 기초 부하 테스트(JMeter 등), 장애 복구 시나리오 테스트 | 클라우드 보안 컴플라이언스 체크리스트 준수 |
| **심화** | **최적화** | 실비용 기반 인프라 사이징 최적화, 성능 튜닝 | FinOps 관점의 비용 절감 리포트 작성 |
|  | **운영** | Self-healing 구조 설계(K8s Liveness/Readiness) | 자동 재학습/배포 파이프라인(MLOps 연동) |
|  | **통합** | 통합 운영 매뉴얼 작성, 인프라 고도화 성과 보고 | 오픈소스 클라우드 네이티브 도구 추가 통합 |

## 🔹 상세 구현 기준

### **📖 기본 프로젝트 —** 자율 주제: 클라우드 네이티브 성능 최적화 및 모니터링

> **서비스 예시**: 라이브 스트리밍 플랫폼 (콘텐츠·스트리밍·사용자·채팅 서비스)
> 

### (1) 성능 최적화 전략 수립

**`필수`**

- k6로 기준 성능을 측정한다(P99 응답시간, 최대 처리량, 에러율). k6 스크립트는 `tests/performance/` 디렉토리에 코드로 관리하여 CI에서 정기 실행한다.
- Prometheus 메트릭 분석으로 CPU·메모리·네트워크 I/O 병목 지점을 서비스별로 식별하고, 병목 원인과 개선 방향을 문서화한다.
- HPA(CPU 기준) 설정 후 동일 k6 시나리오로 오토스케일링 동작을 검증하고 스케일아웃 응답 시간을 측정한다.

**`선택`**

- Scheduled Scaling으로 피크 시간대(예: 저녁 8~11시) 사전 최소 Replica 확장을 설정한다.
- KEDA ScaledObject를 적용하여 큐 기반(Kafka consumer lag 또는 메시지 수) 스케일링을 구현한다.
- JMX Exporter를 사이드카로 배포하여 JVM GC Stop-the-World 발생 횟수와 지속 시간을 Prometheus로 수집한다.

---

### (2) 모니터링 시스템 개선

**`필수`**

- 기존 Prometheus 수집 지표에 서비스 전용 커스텀 지표(동시 접속자 수, 처리량, 도메인별 핵심 지표)를 추가한다.
- Grafana 대시보드를 성능 관점(응답시간·처리량·에러율)과 리소스 관점(CPU·메모리)으로 분리하여 구성하고, 실시간 지표가 1초 간격으로 갱신되는 대시보드를 구성한다.
- 성능 지표 임계치 초과 시 Alertmanager를 통해 Slack 알림이 발송되도록 연동한다.

**`선택`**

- 비즈니스 관점 대시보드(서비스별 핵심 KPI)를 추가로 구성한다.
- Grafana Annotation으로 배포 이벤트를 대시보드에 표시하여 배포와 지표 변화의 상관관계를 시각화한다.

---

### (3) 평가 및 최적화

**`필수`**

- 튜닝 방안 적용 전후를 동일한 k6 시나리오로 비교 측정한다. P99 응답시간·최대 처리량·에러율 개선 수치를 Before/After 표로 정리하고 목표치 달성 여부를 평가한다.
- 최적화 결과를 기술 문서로 작성하여 팀 위키에 공유하고, 운영 환경 적용을 위한 점진적 적용 가이드라인을 작성한다.

**`선택`**

- 미달성 항목에 대해 추가 튜닝 방향을 도출하고 2차 개선 사이클을 진행하여 목표 달성 여부를 재검증한다.
- 성능 최적화 정기 리뷰 프로세스(월 1회 메트릭 기반 재검토)를 수립한다.

### **📖 심화 프로젝트 —** 자율 주제: 마이크로서비스 트래픽 관리 및 장애 대응

> **서비스 예시**:  기본 프로젝트의 서비스를 이어서 고도화
> 

### (1) 트래픽 관리 전략 수립

**`필수`**

- Prometheus 메트릭(30일치)을 분석하여 시간대별·이벤트별 트래픽 패턴을 분류하고, 각 패턴에 적합한 스케일링 전략(Scheduled vs Event-driven)을 수립한다.
- Istio VirtualService로 서비스 트래픽 라우팅 정책을 구성한다. 가중치 기반 트래픽 분배를 적용하고 신규 버전 Canary 라우팅을 구현한다.
- Rate Limiting 정책을 API 게이트웨이 또는 Istio 레벨에서 적용한다.

**`선택`**

- Istio Locality-aware Load Balancing을 설정하여 채팅 서비스 레이턴시를 개선하고 효과를 측정한다.
- 트래픽 관리 정책(VirtualService 가중치, Rate Limit 임계치)에 대한 정기 리뷰 프로세스를 수립한다.

---

### (2) 장애 대응 시스템 구축

**`필수`**

- Circuit Breaker(Resilience4j 또는 Istio outlierDetection)를 구현하여 의존 서비스 장애 시 Graceful Degradation이 동작함을 실제 장애 주입으로 검증한다.
- Alertmanager 알림 → Slack `#incident` 채널 자동 생성 연동을 구성한다.
- 장애 복구 절차를 단계별로 정의한 Runbook을 작성하고 실제 장애 시나리오로 검증한다.

**`선택`**

- Alertmanager Webhook과 자동화 스크립트를 연동하여 에러율 초과 탐지 시 HPA 최소 Replica를 자동으로 확장하는 파이프라인을 구현한다.
- Chaos Mesh를 도입하여 Pod 강제 종료·네트워크 지연 주입 시나리오를 정기적으로 실행하고 SLA 유지 여부를 측정한다.

---

### (3) 평가 및 개선

**`필수`**

- 트래픽 관리 정책 적용 전후의 서비스 안정성(에러율, 응답시간 표준편차, SLA 준수율)을 동일 부하 테스트 시나리오로 비교 측정한다.
- MTTR(평균 복구 시간) 개선 수치를 정량화하고 운영 개선 보고서를 작성한다.

**`선택`**

- 트래픽 관리 및 장애 대응 효과 분석 결과를 팀 발표 자료로 구성한다.
- 이번 프로젝트에서 도출한 최적 설정값과 근거를 다음 프로젝트(실무 통합)를 위한 인수인계 문서로 작성한다.